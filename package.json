{
    "name": "craftpath",
    "version": "0.1.0",
    "type": "module",
    "bin": {
        "craftpath": "./bin/craftpath.ts"
    },
    "scripts": {
        "test": "bun test",
        "typecheck": "tsc --noEmit",
        "lint": "biome ci . --error-on-warnings",
        "format": "biome check . --write",
        "check": "bun run lint && bun run typecheck && bun test"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.5.14",
        "@types/bun": "latest",
        "typescript": "^5"
    },
    "dependencies": {
        "@stricli/core": "^1.3.0",
        "zod": "^4.6.5"
    }
}
