{
  "eval_id": 9,
  "eval_name": "format-rejection-test",
  "prompt": "i've got an acceptance criterion that says 'unsupported formats return 415 before any storage write'. write me the test that proves it. junit 5 and we have testcontainers set up",
  "assertions": [
    "The test exercises a real storage boundary rather than mocking away the storage layer whose behavior is under test",
    "The test asserts the 415 response status",
    "The test separately asserts that nothing was written to storage",
    "The test name states the claim (condition and expected outcome) rather than a generic name such as testUpload",
    "Mentions confirming the test fails against a deliberately broken implementation",
    "The test is runnable on its own as a named selector, not only as part of a suite"
  ]
}
